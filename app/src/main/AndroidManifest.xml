<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lensonmutugi.distributeddrinksstore_customer"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DistributedDrinksStoreCustomer"
        tools:targetApi="31">

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".LoginVsSignupActivity"
            android:exported="false" />

        <!-- Customer Flow -->
        <activity
            android:name=".BranchSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".DrinksSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".PaymentActivity"
            android:exported="false" />

        <activity
            android:name=".ReceiptActivity"
            android:exported="false" />

        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginVsSignupActivity" />
        <activity android:name=".BranchSelectionActivity" />
        <activity android:name=".DrinksSelectionActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".ReceiptActivity" />
        <activity android:name=".SelectPaymentMethodActivity" />
    </application>

</manifest>
